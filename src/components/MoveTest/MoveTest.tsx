import styles from "./MoveTest.module.css";
import React, { useState, useEffect } from "react";

const MoveTest = () => {
  return (
    <div className={styles.container}>
      <div className={styles.square}></div>
    </div>
  );
};

export default MoveTest;
